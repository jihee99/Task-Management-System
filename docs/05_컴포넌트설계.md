# 컴포넌트 설계서

## 📋 문서 개요

| 항목 | 내용 |
|:---:|:---|
| 프로젝트 | 할 일 관리 시스템 (Task Management System) |
| 기술 스택 | React 18/19 + TypeScript |

### 범례

| 구분 | 설명 |
|:---|:---|
| ✅ 필수 | 명세에 명시된 필수 구현 항목 |
| ⚠️ 선택 | 명세에 직접 명시되지 않았으나 UX 보완으로 권장되는 항목 |
| 💡 설계 결정 | 명세에 명시되지 않아 자의적으로 결정한 항목 |

---

## 1. 컴포넌트 분류 원칙

### 1.1 분류 기준

| 구분 | 기준 | 예시 |
|:---|:---|:---|
| **공통 컴포넌트** | 2개 이상 페이지에서 재사용 또는 디자인 시스템 요소 | Button, Modal, Input, Icon |
| **레이아웃 컴포넌트** | 페이지 구조를 정의하는 골격 | AppLayout, ProtectedRoute |
| **페이지 컴포넌트** | 라우트와 1:1 매핑되는 최상위 컴포넌트 | DashboardPage, TaskListPage |
| **기능 컴포넌트** | 특정 도메인/기능에 종속된 컴포넌트 | TaskCard, DeleteConfirmModal |

### 1.2 컴포넌트 쪼개기 기준

```
1. 재사용성: 2곳 이상에서 사용되는가?
2. 단일 책임: 하나의 명확한 역할을 가지는가?
3. 테스트 용이성: 독립적으로 테스트 가능한가?
4. 복잡도 관리: 100줄 이상이면 분리 검토
```

---

## 2. 공통 컴포넌트 (Common/UI)

### 2.1 버튼 (Button) ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 폼 제출, 액션 트리거 |
| **사용 위치** | 로그인 폼, 삭제 모달, 에러 모달 |
| **재사용 근거** | 동일한 활성화/비활성화 스타일 패턴 반복 |

**Props 인터페이스**
```typescript
interface ButtonProps {
  children: React.ReactNode;
  variant?: 'primary' | 'secondary' | 'ghost';
  disabled?: boolean;
  loading?: boolean;
  type?: 'button' | 'submit';
  onClick?: () => void;
  fullWidth?: boolean;
}
```

**스타일 규칙 (디자인 토큰 적용)** ✅
| 상태 | 배경색 | 비고 |
|:---|:---|:---|
| 활성화 | `primary` (blue) | 클릭 가능 |
| 비활성화 | `disabled` (gray) | 클릭 불가, `disabled` 속성 적용 |

---

### 2.2 입력 필드 (Input) ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 텍스트 입력 (이메일, 비밀번호, ID 확인) |
| **사용 위치** | 로그인 폼, 삭제 확인 모달 |
| **재사용 근거** | label + input + error 패턴 반복 |

**Props 인터페이스**
```typescript
interface InputProps {
  id: string;
  label: string;              // ✅ 필수: 명세 "form의 input에 대한 label이 표기되어야 합니다"
  type?: 'text' | 'email' | 'password';
  value: string;
  onChange: (value: string) => void;
  error?: string;             // 유효성 검증 에러 메시지
  placeholder?: string;
  disabled?: boolean;
  required?: boolean;
}
```

**접근성 요구사항**
- `<label htmlFor={id}>` 필수 연결
- 에러 시 `aria-invalid="true"` 적용
- 필수 입력 시 `aria-required="true"` 적용

---

### 2.3 모달 (Modal) ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 오버레이 다이얼로그 (에러, 확인) |
| **사용 위치** | 에러 모달, 삭제 확인 모달 |
| **재사용 근거** | 오버레이 + 중앙 정렬 + 닫기 로직 공통 |

**Props 인터페이스**
```typescript
interface ModalProps {
  isOpen: boolean;
  onClose: () => void;
  title?: string;
  children: React.ReactNode;
  closeOnOverlayClick?: boolean;  // 기본값: true
  closeOnEsc?: boolean;           // 기본값: true
  showCloseButton?: boolean;      // 기본값: true
}
```

**기능 요구사항**
- ESC 키로 닫기
- 오버레이 클릭으로 닫기 (옵션)
- Portal을 사용하여 DOM 최상위에 렌더링
- 포커스 트랩 ⚠️ 선택

---

### 2.4 카드 (Card)

| 항목 | 내용 |
|:---|:---|
| **용도** | 콘텐츠 그룹화 컨테이너 |
| **사용 위치** | 대시보드 통계(StatCard), 할 일 목록 아이템(TaskCard) |
| **재사용 근거** | 일관된 박스 스타일 적용 |

**Props 인터페이스**
```typescript
interface CardProps {
  children: React.ReactNode;
  onClick?: () => void;       // 클릭 가능 여부
  hoverable?: boolean;        // 호버 효과 (onClick 존재 시 자동 true)
  className?: string;
  as?: 'div' | 'article';     // 시맨틱 태그 선택
}
```

**컴포넌트 관계** 💡
```
Card (공통 컨테이너)
├── TaskCard에서 사용 (hoverable, onClick 적용)
└── StatCard에서 사용 (정적, 클릭 불가)
```

---

### 2.5 아이콘 (Icon) ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 메뉴, 버튼 등에 아이콘 표시 |
| **사용 위치** | GNB 메뉴, 대시보드 통계 카드 |
| **재사용 근거** | 일관된 아이콘 시스템 |

**Props 인터페이스**
```typescript
type IconName = 
  | 'dashboard'    // 대시보드 메뉴
  | 'task'         // 할 일 메뉴  
  | 'login'        // 로그인 (비로그인 시)
  | 'user'         // 회원정보 (로그인 시)
  | 'delete'       // 삭제 버튼
  | 'spinner';     // 로딩

interface IconProps {
  name: IconName;
  size?: 'sm' | 'md' | 'lg';  // 16px, 24px, 32px
  className?: string;
}
```

**아이콘 라이브러리** 💡
- 권장: `lucide-react` (경량, Tree-shaking 지원)
- 대안: `react-icons`, `@heroicons/react`

**명세 요구사항** ✅
> "메뉴별 아이콘은 서로 겹치지 않게 지정"

| 위치 | 아이콘 | 설명 |
|:---|:---|:---|
| 대시보드 메뉴 | `LayoutDashboard` | 대시보드 아이콘 |
| 할 일 메뉴 | `CheckSquare` | 할 일 아이콘 |
| 로그인 | `LogIn` | 로그인 아이콘 |
| 회원정보 | `User` | 회원정보 아이콘 |

---

### 2.6 로딩 인디케이터 (LoadingSpinner) ⚠️ 선택

| 항목 | 내용 |
|:---|:---|
| **용도** | API 로딩 상태 표시 |
| **사용 위치** | 모든 API 호출 페이지 |
| **재사용 근거** | 일관된 로딩 UX |

**Props 인터페이스**
```typescript
interface LoadingSpinnerProps {
  size?: 'sm' | 'md' | 'lg';
  fullScreen?: boolean;       // 화면 전체 오버레이
  label?: string;             // 접근성: 스크린리더용 텍스트
}
```

---

### 2.7 빈 상태 (EmptyState) ⚠️ 선택

| 항목 | 내용 |
|:---|:---|
| **용도** | 데이터가 없는 상태 표시 |
| **사용 위치** | 할 일 목록 (빈 목록) |
| **재사용 근거** | 빈 상태 UX 일관성 |

**Props 인터페이스**
```typescript
interface EmptyStateProps {
  icon?: React.ReactNode;
  title: string;
  description?: string;
}
```

---

## 3. 레이아웃 컴포넌트 (Layout)

### 3.1 ProtectedRoute (라우트 가드) ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 인증이 필요한 라우트 보호 |
| **적용 라우트** | `/task`, `/task/:id`, `/user` (보호된 페이지) |
| **제외 라우트** | `/` (대시보드 - 랜딩 페이지), `/sign-in` |
| **책임** | 미인증 시 로그인 페이지로 즉시 리다이렉트, 원래 경로 저장 |

> 💡 **설계 결정**: 대시보드는 비로그인 사용자도 접근 가능한 랜딩 페이지로 활용한다. ProtectedRoute를 적용하지 않고, 페이지 레벨에서 401 시 로그인 유도 UI를 표시한다.

**Props 인터페이스**
```typescript
interface ProtectedRouteProps {
  children: React.ReactNode;
}
```

**동작 흐름** ✅
```
┌─────────────────────────────────────────────────────────────┐
│  ProtectedRoute (보호된 페이지: /task, /task/:id, /user)    │
│       │                                                     │
│       ▼                                                     │
│  isAuthenticated 확인                                       │
│       │                                                     │
│       ├── true → {children} 렌더링                          │
│       │                                                     │
│       └── false                                             │
│            │                                                │
│            ▼                                                │
│       현재 경로 저장 (redirectAfterLoginPath)               │
│            │                                                │
│            ▼                                                │
│       /sign-in 즉시 리다이렉트                              │
└─────────────────────────────────────────────────────────────┘
```

**명세 요구사항**
> "401 에러 발생 시 로그인 페이지로 자동 리다이렉트"
> "로그인 성공 후 원래 접근하려던 페이지로 복귀"

> 💡 **대시보드 제외**: 대시보드(`/`)는 ProtectedRoute를 사용하지 않음. 페이지 레벨에서 401 처리.

---

### 3.2 AppLayout ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | GNB 포함 메인 레이아웃 |
| **적용 라우트** | `/`, `/task`, `/task/:id`, `/user` |
| **제외 라우트** | `/sign-in` |

**구조**
```
┌─────────────────────────────────────────┐
│                 GNB                      │
├─────────────────────────────────────────┤
│                                         │
│              {children}                  │
│           (페이지 콘텐츠)                │
│                                         │
└─────────────────────────────────────────┘
```

**Props 인터페이스**
```typescript
interface AppLayoutProps {
  children: React.ReactNode;
}
```

---

### 3.3 GNB (Global Navigation Bar) ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 전역 네비게이션 |
| **포함 요소** | 라우트 메뉴(대시보드/할 일), 로그인/회원정보 링크 |

**하위 컴포넌트 구조**
```
GNB
├── NavLink (대시보드)
├── NavLink (할 일)
└── 인증 링크 (로그인/회원정보)
```

**상태 분기** ✅
| 인증 상태 | 우측 아이콘 | 클릭 동작 |
|:---|:---|:---|
| 로그인됨 | 회원정보 아이콘 (`user`) | `/user` 이동 |
| 비로그인 | 로그인 아이콘 (`login`) | `/sign-in` 이동 |

---

## 4. 페이지 컴포넌트 (Pages)

### 4.1 페이지 컴포넌트 책임

```
1. 라우트 파라미터 처리 (useParams)
2. 데이터 페칭 (useQuery 호출)
3. 에러/로딩 상태 분기
4. 하위 컴포넌트 조합 및 레이아웃
5. 이벤트 핸들러 정의 및 전달
```

---

### 4.2 SignInPage (`/sign-in`) ✅

| 항목 | 내용 |
|:---|:---|
| **레이아웃** | 단독 레이아웃 (GNB 미포함) |
| **API 호출** | `POST /api/sign-in` |
| **상태 관리** | 로컬 (폼 입력, 유효성, 모달) |

**하위 컴포넌트**
```
SignInPage
├── SignInForm
│   ├── input (email) - label 필수 ✅
│   ├── input (password) - label 필수 ✅
│   └── Button (submit)
└── Modal (조건부, 400 에러 시)
```

**로그인 성공 후 리다이렉트 처리**
```typescript
// SignInPage 내부
const handleSuccess = () => {
  const redirectPath = authStore.redirectAfterLoginPath || '/';
  authStore.clearRedirectPath();
  navigate(redirectPath);
};
```

---

### 4.3 DashboardPage (`/`) ✅ - 랜딩 페이지

| 항목 | 내용 |
|:---|:---|
| **레이아웃** | AppLayout (💡 ProtectedRoute 미적용 - 랜딩 페이지) |
| **API 호출** | `GET /api/dashboard` |
| **인증 필요** | 💡 비로그인 접근 허용 (401 시 로그인 유도 UI) |

> 💡 **설계 결정**: 대시보드는 비로그인 사용자도 접근 가능한 랜딩 페이지 역할을 한다. 401 응답 시 리다이렉트 없이 로그인 유도 UI를 표시한다.

**하위 컴포넌트**
```
DashboardPage
├── DashboardStats (로그인 + 200 응답 시)
│   ├── StatCard (numOfTask) - "일"
│   ├── StatCard (numOfRestTask) - "해야할 일"
│   └── StatCard (numOfDoneTask) - "한 일"
├── LoginPrompt (401 응답 시) 💡
│   └── Button (로그인하기)
└── LoadingSpinner (조건부) ⚠️
```

**401 처리** 💡
| 상태 코드 | 처리 |
|:---|:---|
| 200 | 통계 카드 표시 |
| 401 | 로그인 유도 UI 표시 (리다이렉트 없음) |

---

### 4.4 TaskListPage (`/task`) ✅ - 보호된 페이지

| 항목 | 내용 |
|:---|:---|
| **레이아웃** | AppLayout (ProtectedRoute 적용) |
| **API 호출** | `GET /api/task` (페이지네이션) |
| **인증 필요** | ✅ (401 시 즉시 리다이렉트) |
| **특수 기능** | 가상 스크롤링 ✅, 무한 스크롤 ✅ |

**하위 컴포넌트**
```
TaskListPage
├── VirtualizedTaskList
│   └── TaskCard (반복)
├── EmptyState (데이터 0건 시) ⚠️
├── LoadingSpinner (하단, 추가 로딩 시) ⚠️
└── Modal (400 에러 시)
```

**에러 처리** ✅
| 상태 코드 | 처리 |
|:---|:---|
| 400 | Modal 표시 (errorMessage + 목록으로 이동 버튼) |
| 401 | 로그인 페이지 즉시 리다이렉트 (전역 인터셉터) |

---

### 4.5 TaskDetailPage (`/task/:id`) ✅ - 보호된 페이지

| 항목 | 내용 |
|:---|:---|
| **레이아웃** | AppLayout (ProtectedRoute 적용) |
| **API 호출** | `GET /api/task/:id` |
| **인증 필요** | ✅ (401 시 즉시 리다이렉트) |
| **에러 처리** | 404 → 404 화면 렌더링 ✅ |

**하위 컴포넌트**
```
TaskDetailPage
├── TaskDetail
│   ├── title
│   ├── memo
│   ├── registerDatetime
│   └── Button (삭제)
├── DeleteConfirmModal (조건부)
└── LoadingSpinner (조건부) ⚠️
```

**id 처리** 💡
```typescript
// TaskDetailPage에서 URL 파라미터로 id 획득
const { id } = useParams<{ id: string }>();

// API 응답과 합성하여 TaskDetail에 전달
<TaskDetail 
  id={id}
  task={data}  // { title, memo, registerDatetime }
  onDeleteClick={handleDeleteClick}
/>
```

---

### 4.6 UserPage (`/user`) ✅ - 보호된 페이지

| 항목 | 내용 |
|:---|:---|
| **레이아웃** | AppLayout (ProtectedRoute 적용) |
| **API 호출** | `GET /api/user` |
| **인증 필요** | ✅ (401 시 즉시 리다이렉트) |

**하위 컴포넌트**
```
UserPage
├── UserProfile
│   ├── name
│   └── memo
└── LoadingSpinner (조건부) ⚠️
```

---

## 5. 기능 컴포넌트 (Features)

### 5.1 SignInForm ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 로그인 폼 로직 캡슐화 |
| **책임** | 입력값 관리, 유효성 검증, 제출 처리 |

**유효성 규칙** ✅
| 필드 | 규칙 | 에러 메시지 (예시) |
|:---|:---|:---|
| email | RFC 이메일 규약 (필수) | "올바른 이메일 형식이 아닙니다" |
| password | 영문/한글/숫자**만** 허용, 8~24자 (필수) | "비밀번호는 영문, 한글, 숫자로 8~24자여야 합니다" |

> ⚠️ **주의**: 비밀번호는 특수문자 불가 (명세: "영문, 한글, 숫자로 구성된")

**Props 인터페이스**
```typescript
interface SignInFormProps {
  onSuccess: () => void;
  onError: (errorMessage: string) => void;
}
```

**버튼 상태** ✅
| 조건 | 버튼 상태 | 배경색 |
|:---|:---|:---|
| 모든 유효성 통과 | 활성화 | `primary` (blue) |
| 유효성 미통과 또는 빈 값 | 비활성화 | `disabled` (gray) |

---

### 5.2 TaskCard ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 할 일 목록 아이템 |
| **책임** | title, memo 표시, 클릭 시 상세 이동 |
| **내부 사용** | Card 컴포넌트 (hoverable, onClick 적용) |

**Props 인터페이스**
```typescript
interface TaskCardProps {
  id: string;           // 💡 API Mock에서 추가 또는 배열 인덱스 사용
  title: string;
  memo: string;
  onClick: (id: string) => void;
}
```

**API 응답과의 관계** 💡
```typescript
// API 응답 (명세)
interface TaskListItemResponse {
  title: string;
  memo: string;
  status: 'TODO' | 'DONE';
  // id 없음!
}

// Mock API에서 id 추가 권장
interface TaskListItem extends TaskListItemResponse {
  id: string;  // Mock에서 UUID 또는 인덱스 기반 생성
}
```

---

### 5.3 VirtualizedTaskList ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 가상 스크롤링 + 무한 스크롤 통합 |
| **책임** | 뷰포트 내 아이템만 렌더링, 스크롤 끝 감지 |
| **라이브러리** | `@tanstack/react-virtual` 권장 💡 |

**Props 인터페이스**
```typescript
interface VirtualizedTaskListProps {
  tasks: TaskListItem[];
  itemHeight: number;         // 가상 스크롤 필수 (예: 80)
  onLoadMore: () => void;
  hasNextPage: boolean;
  isLoadingMore: boolean;
  onTaskClick: (id: string) => void;
}
```

**명세 요구사항** ✅
> "스크롤 영역에서 화면에 보여지는 요소 또는 보여질 요소에 대해서만 적절히 렌더링해주세요(가상 스크롤링)"
> "목록에 끝에 도달하는 경우 다음 페이지의 API를 호출해주세요(무한 스크롤)"

---

### 5.4 TaskDetail ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 할 일 상세 정보 표시 |
| **책임** | title, memo, registerDatetime 표시, 삭제 버튼 |

**Props 인터페이스**
```typescript
interface TaskDetailProps {
  id: string;                 // URL 파라미터에서 전달
  task: {
    title: string;
    memo: string;
    registerDatetime: string;
  };
  onDeleteClick: () => void;
}
```

---

### 5.5 DeleteConfirmModal ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 삭제 확인 모달 (ID 입력 검증) |
| **책임** | ID 입력값과 실제 ID 비교, 제출 버튼 활성화 제어 |

**Props 인터페이스**
```typescript
interface DeleteConfirmModalProps {
  isOpen: boolean;
  onClose: () => void;
  targetId: string;
  onConfirm: () => void;
}
```

**내부 구조**
```typescript
// DeleteConfirmModal 내부
<Modal isOpen={isOpen} onClose={onClose} title="삭제 확인">
  <Input 
    id="delete-confirm-input"
    label="삭제하려면 ID를 입력하세요"  // ✅ label 필수
    value={inputValue}
    onChange={setInputValue}
  />
  <Button 
    disabled={inputValue !== targetId}  // 파생 상태
    onClick={onConfirm}
  >
    삭제
  </Button>
</Modal>
```

**내부 상태**
- `inputValue`: 입력된 ID 값
- `isValid`: `inputValue === targetId` (파생)

**버튼 상태** ✅
| 조건 | 버튼 상태 | 배경색 |
|:---|:---|:---|
| ID 일치 | 활성화 | `primary` (blue) |
| ID 불일치 또는 빈 값 | 비활성화 | `disabled` (gray) |

---

### 5.6 DashboardStats ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 대시보드 통계 영역 |
| **책임** | 3개 통계 카드 레이아웃 |

**Props 인터페이스**
```typescript
interface DashboardStatsProps {
  numOfTask: number;
  numOfRestTask: number;
  numOfDoneTask: number;
}
```

---

### 5.7 StatCard ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 개별 통계 표시 카드 |
| **재사용** | DashboardStats 내에서 3회 사용 |
| **내부 사용** | Card 컴포넌트 (정적, 클릭 불가) |

**Props 인터페이스**
```typescript
interface StatCardProps {
  label: string;              // "일", "해야할 일", "한 일"
  value: number;
  icon?: React.ReactNode;
}
```

---

### 5.8 LoginPrompt 💡

| 항목 | 내용 |
|:---|:---|
| **용도** | 비로그인 사용자에게 로그인 유도 |
| **사용 위치** | DashboardPage (401 응답 시) |
| **책임** | 로그인 안내 메시지 + 로그인 버튼 표시 |

> 💡 **설계 결정**: 대시보드는 비로그인 사용자도 접근 가능한 랜딩 페이지 역할을 한다. 401 응답 시 이 컴포넌트를 표시하여 로그인을 유도한다.

**Props 인터페이스**
```typescript
interface LoginPromptProps {
  title?: string;             // 기본: "로그인이 필요합니다"
  message?: string;           // 기본: "할 일 현황을 확인하려면 로그인하세요"
  buttonLabel?: string;       // 기본: "로그인하기"
}
```

**내부 구조**
```typescript
// LoginPrompt 내부
<div className="login-prompt">
  <h2>{title}</h2>
  <p>{message}</p>
  <Button onClick={() => navigate('/sign-in')}>
    {buttonLabel}
  </Button>
</div>
```

---

### 5.9 UserProfile ✅

| 항목 | 내용 |
|:---|:---|
| **용도** | 회원 정보 표시 |
| **책임** | name, memo 표시 |

**Props 인터페이스**
```typescript
interface UserProfileProps {
  name: string;
  memo: string;
}
```

---

## 6. 컴포넌트 트리 (전체 구조)

```
App
├── AuthProvider (Provider 조합용 래퍼)
├── QueryClientProvider (React Query)
│
├── Routes
│   │
│   ├── /sign-in (인증 불필요)
│   │   └── SignInPage
│   │       ├── SignInForm
│   │       │   ├── input (email) - label ✅
│   │       │   ├── input (password) - label ✅
│   │       │   └── Button
│   │       └── Modal
│   │
│   ├── / (대시보드 - 랜딩 페이지, ProtectedRoute 미적용) 💡
│   │   └── AppLayout
│   │       ├── GNB
│   │       │   ├── NavLink (대시보드)
│   │       │   ├── NavLink (할 일)
│   │       │   └── 인증 링크
│   │       │
│   │       └── DashboardPage
│   │           ├── DashboardStats (로그인 + 200 시)
│   │           │   └── StatCard (x3)
│   │           │       └── Card
│   │           ├── LoginPrompt (401 시) 💡
│   │           │   └── Button (로그인하기)
│   │           └── LoadingSpinner ⚠️
│   │
│   └── ProtectedRoute (보호된 페이지: /task, /task/:id, /user) ✅
│       └── AppLayout
│           ├── GNB
│           │   ├── NavLink (대시보드)
│           │   ├── NavLink (할 일)
│           │   └── 인증 링크
│           │
│           └── {children}
│               │
│               ├── /task → TaskListPage
│               │   ├── VirtualizedTaskList
│               │   │   └── TaskCard (x N)
│               │   │       └── Card
│               │   ├── EmptyState ⚠️
│               │   ├── LoadingSpinner ⚠️
│               │   └── Modal
│               │
│               ├── /task/:id → TaskDetailPage
│               │   ├── TaskDetail
│               │   │   └── Button (삭제)
│               │   ├── DeleteConfirmModal
│               │   │   ├── Modal
│               │   │   ├── Input - label ✅
│               │   │   └── Button
│               │   └── LoadingSpinner ⚠️
│               │
│               └── /user → UserPage
│                   ├── UserProfile
│                   └── LoadingSpinner ⚠️
```

---

## 7. 폴더 구조 제안

```
src/
├── components/
│   ├── common/                 # 공통 UI 컴포넌트
│   │   ├── Button/
│   │   │   ├── Button.tsx
│   │   │   ├── Button.styles.ts (또는 .module.css)
│   │   │   └── index.ts
│   │   ├── Input/
│   │   ├── Modal/
│   │   ├── Card/
│   │   ├── Icon/
│   │   ├── LoadingSpinner/
│   │   └── EmptyState/
│   │
│   ├── layout/                 # 레이아웃 컴포넌트
│   │   ├── AppLayout/
│   │   ├── GNB/
│   │   │   └── GNB.tsx
│   │   └── ProtectedRoute/
│   │
│   └── feedback/               # 상태/피드백 컴포넌트
│       └── ErrorModal/
│
├── features/                   # 기능별 컴포넌트
│   ├── auth/
│   │   └── SignInForm/
│   │
│   ├── dashboard/
│   │   ├── DashboardStats/
│   │   └── StatCard/
│   │
│   ├── task/
│   │   ├── TaskCard/
│   │   ├── TaskDetail/
│   │   ├── VirtualizedTaskList/
│   │   └── DeleteConfirmModal/
│   │
│   └── user/
│       └── UserProfile/
│
├── pages/                      # 페이지 컴포넌트
│   ├── SignInPage.tsx
│   ├── DashboardPage.tsx
│   ├── TaskListPage.tsx
│   ├── TaskDetailPage.tsx
│   └── UserPage.tsx
│
├── hooks/                      # 커스텀 훅
│   ├── useAuth.ts
│   └── queries/
│       ├── useDashboard.ts
│       ├── useTasks.ts         # useInfiniteQuery
│       ├── useTask.ts
│       └── useUser.ts
│
├── contexts/                   # Context 정의
│   └── AuthContext.tsx
│
├── api/                        # API 클라이언트
│   ├── client.ts               # Axios 인스턴스 + 인터셉터
│   └── endpoints/
│       ├── auth.ts
│       ├── dashboard.ts
│       ├── task.ts
│       └── user.ts
│
├── mocks/                      # MSW Mock
│   ├── handlers/
│   │   ├── auth.ts
│   │   ├── dashboard.ts
│   │   ├── task.ts
│   │   └── user.ts
│   ├── browser.ts
│   └── server.ts
│
├── styles/                     # 디자인 토큰
│   ├── tokens.ts
│   └── global.css
│
├── types/                      # 타입 정의
│   ├── api.ts
│   └── components.ts
│
└── utils/                      # 유틸리티
    └── validation.ts           # 이메일, 비밀번호 검증
```

---

## 8. 디자인 토큰 적용 ✅

### 8.1 색상 토큰

```typescript
// styles/tokens.ts
export const colors = {
  // 필수 토큰 ✅
  primary: '#3B82F6',       // blue - 활성화된 버튼
  disabled: '#9CA3AF',      // gray - 비활성화된 버튼
  
  // 확장 토큰 (선택)
  background: '#FFFFFF',
  surface: '#F9FAFB',
  text: {
    primary: '#1F2937',
    secondary: '#6B7280',
  },
  error: '#EF4444',
  border: '#E5E7EB',
} as const;

export type ColorToken = keyof typeof colors;
```

### 8.2 폰트 ✅

```css
/* styles/global.css */
@import url('https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css');

body {
  font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, sans-serif;
}
```

---

## 9. 컴포넌트 요약 테이블

### 9.1 공통 컴포넌트 (7개)

| 컴포넌트 | 필수 | 재사용 횟수 | 주요 Props |
|:---|:---:|:---:|:---|
| Button | ✅ | 5+ | variant, disabled, loading |
| Input | ✅ | 3+ | label ✅, error, type |
| Modal | ✅ | 2 | isOpen, onClose, children |
| Card | - | 4+ | onClick, hoverable |
| Icon | ✅ | 6+ | name, size |
| LoadingSpinner | ⚠️ | 4+ | size, fullScreen |
| EmptyState | ⚠️ | 1 | title, description |

### 9.2 레이아웃 컴포넌트 (3개)

| 컴포넌트 | 필수 | 주요 책임 |
|:---|:---:|:---|
| ProtectedRoute | ✅ | 인증 확인, 리다이렉트 |
| AppLayout | ✅ | GNB + 콘텐츠 영역 |
| GNB | ✅ | 전역 네비게이션 |

### 9.3 기능 컴포넌트 (9개)

| 컴포넌트 | 페이지 | 주요 책임 |
|:---|:---|:---|
| SignInForm | SignInPage | 폼 유효성 ✅, 제출 |
| TaskCard | TaskListPage | 목록 아이템 (Card 사용) |
| VirtualizedTaskList | TaskListPage | 가상 스크롤 ✅, 무한 스크롤 ✅ |
| TaskDetail | TaskDetailPage | 상세 정보 표시 |
| DeleteConfirmModal | TaskDetailPage | 삭제 확인 (ID 검증) ✅ |
| DashboardStats | DashboardPage | 통계 레이아웃 |
| StatCard | DashboardPage | 개별 통계 (Card 사용) |
| LoginPrompt | DashboardPage | 💡 비로그인 시 로그인 유도 |
| UserProfile | UserPage | 회원 정보 표시 |

---

## 10. 구현 체크리스트

### 10.1 공통 컴포넌트

- [ ] Button (primary/disabled 스타일) ✅
- [ ] Input (label 필수 + error 표시) ✅
- [ ] Modal (Portal, ESC 닫기) ✅
- [ ] Card (클릭 가능 여부)
- [ ] Icon (lucide-react 통합) ✅
- [ ] LoadingSpinner ⚠️
- [ ] EmptyState ⚠️

### 10.2 레이아웃 컴포넌트

- [ ] ProtectedRoute (인증 확인, 리다이렉트) ✅
- [ ] AppLayout (GNB 포함) ✅
- [ ] GNB (인증 상태 분기) ✅

### 10.3 페이지 컴포넌트

- [ ] SignInPage ✅
- [ ] DashboardPage ✅
- [ ] TaskListPage (가상 스크롤 + 무한 스크롤) ✅
- [ ] TaskDetailPage (삭제 모달) ✅
- [ ] UserPage ✅

### 10.4 기능 컴포넌트

- [ ] SignInForm (유효성: 이메일 RFC, 비밀번호 영문/한글/숫자만 8~24자) ✅
- [ ] TaskCard ✅
- [ ] VirtualizedTaskList (itemHeight 필수) ✅
- [ ] TaskDetail ✅
- [ ] DeleteConfirmModal (ID 검증, Input label 포함) ✅
- [ ] DashboardStats / StatCard ✅
- [ ] LoginPrompt (대시보드 401 시 표시) 💡
- [ ] UserProfile ✅

### 10.5 디자인 시스템

- [ ] 색상 토큰 (primary: blue, disabled: gray) ✅
- [ ] Pretendard 폰트 적용 ✅

### 10.6 API Mock 보완

- [ ] GET /api/task 응답에 `id` 필드 추가 💡
- [ ] 페이지네이션 메타 정보 (`hasNextPage` 판단 기준) 💡

---

## 11. 설계 결정 사항

| # | 결정 사항 | 선택                                 | 근거 |
|:---|:---|:-----------------------------------|:---|
| 1 | 대시보드 접근 권한 | 비로그인 허용 (랜딩 페이지)                   | 원본 명세에 401 처리 미명시 |
| 2 | 대시보드 401 처리 | 로그인 유도 UI (리다이렉트 없음)               | 보호된 페이지와 차별화 |
| 3 | Modal 구현 방식 | React Portal                       | z-index 관리, DOM 최상위 렌더링 |
| 4 | 가상 스크롤 라이브러리 | `@tanstack/react-virtual`          | 경량, 최신 유지보수, 무한 스크롤 호환 |
| 5 | 폼 관리 | React Hook Form                    | 유효성 검증 통합, 성능 최적화 |
| 6 | 전역 상태 관리 | Zustand                            | 인증 상태만 관리, 단순성 |
| 7 | 서버 상태 관리 | React Query                        | 캐싱, 리페칭, useInfiniteQuery |
| 8 | 스타일링 | CSS Modules 또는 Tailwind            | 컴포넌트 스코프 스타일 |
| 9 | 아이콘 라이브러리 | lucide-react                       | 경량, Tree-shaking |
| 10 | Task id 처리 | Mock API에서 UUID 생성                 | 상세 페이지 이동 필수 |
| 11 | 보호된 페이지 401 처리 | Axios Interceptor + ProtectedRoute | 중앙 집중 처리 |
| 12 | 로그인 페이지 레이아웃 | GNB 미포함 (단독 레이아웃)               | 로그인 집중 UX |

---

## 12. API Mock 보완 사항 💡

### 12.1 GET /api/task 응답 수정

**현재 명세**:
```typescript
{
  title: string;
  memo: string;
  status: 'TODO' | 'DONE';
}
```

**Mock 구현 시 추가**:
```typescript
{
  id: string;        // UUID 또는 고유 식별자
  title: string;
  memo: string;
  status: 'TODO' | 'DONE';
}
```

### 12.2 페이지네이션 처리

**무한 스크롤을 위한 판단 기준**:
```typescript
// 옵션 A: 응답 배열 길이로 판단
const PAGE_SIZE = 20;
const hasNextPage = data.length === PAGE_SIZE;

// 옵션 B: 메타 정보 추가 (Mock 확장)
interface TaskListResponse {
  items: TaskListItem[];
  hasNextPage: boolean;
  totalCount: number;
}
```

---
