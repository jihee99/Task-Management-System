# 폴더 구조 & 프론트엔드 아키텍처 설계서

## 📋 문서 개요

| 항목 | 내용 |
|:---:|:---|
| 프로젝트 | 할 일 관리 시스템 (Task Management System) |
| 기술 스택 | React 18/19 + TypeScript |
| 문서 버전 | v1.0 |

---

## 1. 아키텍처 설계 원칙

### 핵심 질문과 답변

| 질문 | 답변 | 근거 |
|:---|:---|:---|
| 도메인 기준 vs 역할 기준? | **하이브리드** (역할 기반 + 도메인 그룹핑) | 프로젝트 규모에 적합, 확장성 확보 |
| 확장 시 어디에 추가? | 도메인별 `features/` 폴더 | 새 기능 추가 시 영향 범위 최소화 |
| 프로젝트가 커져도 버틸 수 있는가? | ✅ 3단계 확장 전략 수립 | 현재 → 중기 → 장기 구조 정의 |

### 1.1 설계 원칙

```
1. 관심사 분리 (Separation of Concerns)
   - UI / 비즈니스 로직 / 데이터 레이어 분리

2. 단방향 의존성 (Unidirectional Dependency)
   - pages → features → components → (외부 의존성 없음)
   - 하위 레이어는 상위 레이어를 모름

3. 코로케이션 (Colocation)
   - 관련 파일은 가까이 배치 (컴포넌트 + 스타일 + 테스트)

4. 명시적 Public API
   - 각 모듈은 index.ts를 통해 외부 노출 제어
```

### 1.2 구조 선택: Feature-Sliced 변형

**선택 이유**:

| 대안 | 장점 | 단점 | 본 프로젝트 적합성 |
|:---|:---|:---|:---:|
| **Flat 구조** | 단순함 | 규모 커지면 혼란 | ❌ |
| **역할 기반** (components/, hooks/) | 익숙함, 직관적 | 도메인 응집도 낮음 | ⚠️ |
| **도메인 기반** (features/) | 높은 응집도 | 공통 요소 중복 가능 | ⚠️ |
| **Feature-Sliced 변형** | 역할 + 도메인 장점 결합 | 초기 학습 비용 | ✅ 채택 |

---

## 2. 디렉토리 트리

### 2.1 전체 구조

```
src/
│
├── 📁 app/                     # 앱 진입점 및 프로바이더
│   ├── App.tsx                 # 루트 컴포넌트
│   ├── Router.tsx              # 라우트 정의
│   └── providers/              # 전역 프로바이더 조합
│       ├── index.tsx           # 프로바이더 통합
│       ├── AuthProvider.tsx
│       └── QueryProvider.tsx
│
├── 📁 pages/                   # 라우트 1:1 매핑 페이지
│   ├── SignInPage.tsx
│   ├── DashboardPage.tsx
│   ├── TaskListPage.tsx
│   ├── TaskDetailPage.tsx
│   └── UserPage.tsx
│
├── 📁 features/                # 도메인별 기능 모듈
│   ├── 📁 auth/
│   │   ├── components/
│   │   │   └── SignInForm/
│   │   └── index.ts            # Public API
│   │
│   ├── 📁 dashboard/
│   │   ├── components/
│   │   │   ├── DashboardStats/
│   │   │   ├── StatCard/
│   │   ├── hooks/
│   │   │   └── useDashboard.ts
│   │   └── index.ts
│   │
│   ├── 📁 task/
│   │   ├── components/
│   │   │   ├── TaskCard/
│   │   │   ├── TaskDetail/
│   │   │   ├── VirtualizedTaskList/
│   │   │   └── DeleteConfirmModal/
│   │   ├── hooks/
│   │   │   ├── useTasks.ts       # useInfiniteQuery
│   │   │   └── useTask.ts
│   │   └── index.ts
│   │
│   └── 📁 user/
│       ├── components/
│       │   └── UserProfile/
│       ├── hooks/
│       │   └── useUser.ts
│       └── index.ts
│
├── 📁 components/              # 공통 UI 컴포넌트
│   ├── 📁 common/              # 범용 UI
│   │   ├── Button/
│   │   │   ├── Button.tsx
│   │   │   ├── Button.module.css
│   │   │   ├── Button.test.tsx
│   │   │   └── index.ts
│   │   ├── Input/
│   │   ├── Modal/
│   │   ├── Card/
│   │   ├── Icon/
│   │   ├── LoginPrompt/
│   │   ├── LoadingSpinner/
│   │   └── EmptyState/
│   │
│   ├── 📁 layout/              # 레이아웃
│   │   ├── AppLayout/
│   │   ├── GNB/
│   │   │   └── GNB.tsx
│   │   └── ProtectedRoute/
│   │
│   └── 📁 feedback/            # 피드백 UI
│       ├── ErrorModal/
│
├── 📁 hooks/                   # 범용 커스텀 훅
│   ├── useAuth.ts              # 인증 상태 접근
│
├── 📁 contexts/                # React Context
│   └── AuthContext.tsx
│
├── 📁 api/                     # API 클라이언트 설정
│   ├── client.ts               # Axios 인스턴스
│   ├── interceptors.ts         # 요청/응답 인터셉터
│   └── types.ts                # API 공통 타입
│
├── 📁 mocks/                   # MSW Mock 서버
│   ├── handlers/
│   │   ├── auth.ts
│   │   ├── dashboard.ts
│   │   ├── task.ts
│   │   └── user.ts
│   ├── data/                   # Mock 데이터
│   │   └── tasks.ts
│   ├── browser.ts              # 브라우저 환경
│   └── server.ts               # 테스트 환경
│
├── 📁 styles/                  # 전역 스타일
│   ├── tokens.ts               # 디자인 토큰
│   ├── global.css              # 전역 CSS
│   └── reset.css               # CSS 리셋
│
├── 📁 types/                   # 전역 타입 정의
│   ├── api.ts                  # API 응답 타입
│   ├── auth.ts                 # 인증 관련 타입
│   └── common.ts               # 공통 유틸 타입
│
├── 📁 utils/                   # 유틸리티 함수
│   ├── validation.ts           # 유효성 검증
│   ├── format.ts               # 포맷팅 (날짜 등)
│   └── storage.ts              # localStorage 래퍼
│
├── 📁 constants/               # 상수 정의
│   ├── routes.ts               # 라우트 경로
│   ├── queryKeys.ts            # React Query 키
│   └── messages.ts             # 에러/안내 메시지
│
└── 📁 test/                    # 테스트 설정
    ├── setup.ts
    └── utils.tsx               # 테스트 유틸리티
```

### 2.2 컴포넌트 내부 구조 (코로케이션)

```
Button/
├── Button.tsx              # 컴포넌트 구현
├── Button.module.css       # 스타일 (CSS Modules)
├── Button.test.tsx         # 테스트 (선택)
├── Button.stories.tsx      # 스토리북 (선택)
└── index.ts                # Public export
```

**index.ts 예시**:
```typescript
// components/common/Button/index.ts
export { Button } from './Button';
export type { ButtonProps } from './Button';
```

---

## 3. 레이어 아키텍처

### 3.1 레이어 다이어그램

```
┌─────────────────────────────────────────────────────────────┐
│                        📱 Pages                              │
│  (라우트 매핑, 페이지 조합, 레이아웃 적용)                    │
│                                                              │
│  SignInPage, DashboardPage, TaskListPage, ...               │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ uses
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      🧩 Features                             │
│  (도메인별 비즈니스 로직, 컴포넌트, 훅, API)                  │
│                                                              │
│  auth/, dashboard/, task/, user/                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ uses
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     🎨 Components                            │
│  (재사용 가능한 UI 컴포넌트, 레이아웃)                        │
│                                                              │
│  common/, layout/, feedback/                                │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ uses
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     🔧 Shared                                │
│  (훅, 유틸, 타입, 상수, API 클라이언트)                       │
│                                                              │
│  hooks/, utils/, types/, constants/, api/, contexts/        │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 의존성 규칙

| From | To | 허용 여부 | 설명 |
|:---|:---|:---:|:---|
| pages | features | ✅ | 페이지가 기능 모듈 사용 |
| pages | components | ✅ | 페이지가 공통 컴포넌트 사용 |
| features | components | ✅ | 기능이 공통 컴포넌트 사용 |
| features | features | ⚠️ | 최소화 (필요시 shared로 추출) |
| components | features | ❌ | 공통 컴포넌트는 도메인 무관 |
| shared | * | ❌ | shared는 의존성 없음 |

### 3.3 데이터 흐름

```
┌─────────────────────────────────────────────────────────────┐
│                         Page                                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ const { data } = useTasks();  // features/task/hooks │    │
│  │ return <TaskListPage tasks={data} />                 │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    features/task/hooks                       │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ export const useTasks = () => {                      │    │
│  │   return useInfiniteQuery({                          │    │
│  │     queryKey: QUERY_KEYS.tasks,                      │    │
│  │     queryFn: fetchTasks,                             │    │
│  │   });                                                │    │
│  │ };                                                   │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       api/client                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Axios Instance + Interceptors                        │    │
│  │ - Authorization Header 자동 첨부                     │    │
│  │ - 401 에러 시 로그인 리다이렉트 (대시보드 제외)       │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 모듈별 책임 정의

### 4.1 app/

| 파일 | 책임 |
|:---|:---|
| `App.tsx` | 루트 컴포넌트, 프로바이더 래핑 |
| `Router.tsx` | 라우트 정의, ProtectedRoute 적용 (보호된 페이지만) |
| `providers/` | 전역 프로바이더 조합 (Auth, Query) |

> 💡 **설계 결정**: 대시보드(`/`)는 ProtectedRoute를 적용하지 않음. 비로그인 사용자도 접근 가능한 랜딩 페이지로 활용.

**App.tsx 예시**:
```typescript
// app/App.tsx
import { Providers } from './providers';
import { Router } from './Router';

export const App = () => (
  <Providers>
    <Router />
  </Providers>
);
```

---

### 4.2 pages/

| 책임 | 설명 |
|:---|:---|
| 라우트 매핑 | URL과 1:1 대응 |
| 레이아웃 적용 | AppLayout 적용 |
| 데이터 페칭 트리거 | features 훅 호출 |
| 상태 분기 | 로딩/에러/정상 렌더링 분기 |
| 이벤트 핸들러 정의 | 하위 컴포넌트에 전달 |

**규칙**:
- 비즈니스 로직 직접 구현 ❌
- UI 로직은 features 컴포넌트에 위임
- 100줄 이하 유지 권장

**TaskListPage 예시**:
```typescript
// pages/TaskListPage.tsx
import React, { useEffect, useState } from 'react';
import { AppLayout } from '@/components/layout';
import { VirtualizedTaskList, useTasks } from '@/features/task';
import { Modal } from '@/components/common/Modal';

export const TaskListPage = () => {
  const { 
    data, 
    fetchNextPage, 
    hasNextPage, 
    isLoading,
    error 
  } = useTasks();
  const [errorOpen, setErrorOpen] = useState(false);

  const handleTaskClick = (id: string) => {
    navigate(`/task/${id}`);
  };

  if (isLoading) return <LoadingSpinner fullScreen />;

  useEffect(() => {
    if (error) setErrorOpen(true);
  }, [error]);

  return (
    <AppLayout>
      <VirtualizedTaskList
        tasks={data?.pages.flat() ?? []}
        onLoadMore={fetchNextPage}
        hasNextPage={hasNextPage}
        onTaskClick={handleTaskClick}
      />
      <Modal isOpen={errorOpen} onClose={() => setErrorOpen(false)} title="오류 발생">
        {error?.message}
      </Modal>
    </AppLayout>
  );
};
```

---

### 4.3 features/

**구조 원칙**: 도메인별 자체 완결성

```
features/task/
├── components/     # 도메인 전용 컴포넌트
├── hooks/          # 도메인 전용 훅 (React Query 래핑)
└── index.ts        # Public API (외부 노출 제어)
```

**index.ts (Public API) 예시**:
```typescript
// features/task/index.ts

// Components
export { TaskCard } from './components/TaskCard';
export { TaskDetail } from './components/TaskDetail';
export { VirtualizedTaskList } from './components/VirtualizedTaskList';
export { DeleteConfirmModal } from './components/DeleteConfirmModal';

// Hooks
export { useTasks } from './hooks/useTasks';
export { useTask } from './hooks/useTask';

// Types (필요시)
export type { TaskListItem, TaskDetail } from './types';
```

**import 규칙**:
```typescript
// ✅ Good: Public API를 통한 import
import { TaskCard, useTasks } from '@/features/task';

// ❌ Bad: 내부 구조 직접 import
import { TaskCard } from '@/features/task/components/TaskCard/TaskCard';
```

---

### 4.4 components/

| 폴더 | 책임 | 예시 |
|:---|:---|:---|
| `common/` | 범용 UI 컴포넌트 | Button, Input, Modal, Card |
| `layout/` | 페이지 구조 | AppLayout, GNB, ProtectedRoute |
| `feedback/` | 상태 피드백 | ErrorModal |

**특징**:
- 도메인 무관 (재사용 가능)
- Props를 통해 동작 주입
- 디자인 토큰 사용

**ProtectedRoute 적용 범위**:
> 💡 보호된 페이지(`/task`, `/task/:id`, `/user`)에만 적용. 대시보드(`/`)는 제외.

---

### 4.5 hooks/

| 훅 | 책임 | 사용 위치 |
|:---|:---|:---|
| `useAuth` | 인증 상태 및 액션 접근 | 전역 |

**도메인 훅 vs 범용 훅**:
```
hooks/              # 범용 (도메인 무관)
└── useAuth.ts

features/task/hooks/  # 도메인 전용
├── useTasks.ts
└── useTask.ts
```

---

### 4.6 api/

| 파일 | 책임 |
|:---|:---|
| `client.ts` | Axios 인스턴스 생성, 기본 설정 |
| `interceptors.ts` | 요청/응답 인터셉터 (인증, 에러 처리) |
| `types.ts` | API 공통 타입 (ErrorResponse 등) |

**client.ts 예시**:
```typescript
// api/client.ts
import axios from 'axios';
import { setupInterceptors } from './interceptors';

export const client = axios.create({
  baseURL: '/api',
  timeout: 10000,
});

setupInterceptors(client);
```

**interceptors.ts 예시**:
```typescript
// api/interceptors.ts
export const setupInterceptors = (client: AxiosInstance) => {
  // 요청 인터셉터: Authorization 헤더 추가
  client.interceptors.request.use((config) => {
    const token = getAccessToken();
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  });

  // 응답 인터셉터: 401 처리 (대시보드 제외)
  client.interceptors.response.use(
    (response) => response,
    (error) => {
      if (error.response?.status === 401) {
        const currentPath = window.location.pathname;
        
        // 💡 대시보드는 401 리다이렉트 제외 (랜딩 페이지)
        // 페이지 레벨에서 로그인 유도 UI 표시
        if (currentPath === '/') {
          return Promise.reject(error);
        }
        
        // 보호된 페이지: 즉시 로그인 리다이렉트
        authStore.logout();
        window.location.href = '/sign-in';
      }
      return Promise.reject(error);
    }
  );
};
```

---

### 4.7 mocks/

| 파일/폴더 | 책임 |
|:---|:---|
| `handlers/` | API 엔드포인트별 Mock 핸들러 |
| `data/` | Mock 데이터 (tasks, users 등) |
| `browser.ts` | 개발 환경 MSW 설정 |
| `server.ts` | 테스트 환경 MSW 설정 |

**handler 예시**:
```typescript
// mocks/handlers/task.ts
import { http, HttpResponse } from 'msw';
import { mockTasks } from '../data/tasks';

export const taskHandlers = [
  http.get('/api/task', ({ request }) => {
    const url = new URL(request.url);
    const page = Number(url.searchParams.get('page')) || 1;
    const pageSize = 20;
    const start = (page - 1) * pageSize;
    const items = mockTasks.slice(start, start + pageSize);
    
    return HttpResponse.json(items);
  }),

  http.get('/api/task/:id', ({ params }) => {
    const task = mockTasks.find(t => t.id === params.id);
    if (!task) {
      return HttpResponse.json(
        { errorMessage: '할 일을 찾을 수 없습니다' },
        { status: 404 }
      );
    }
    return HttpResponse.json(task);
  }),
];
```

---

### 4.8 constants/

| 파일 | 내용 | 예시 |
|:---|:---|:---|
| `routes.ts` | 라우트 경로 상수 | `ROUTES.TASK_DETAIL` |
| `queryKeys.ts` | React Query 키 | `QUERY_KEYS.tasks` |
| `messages.ts` | UI 메시지 | 에러 메시지, 안내 문구 |

**routes.ts 예시**:
```typescript
// constants/routes.ts
export const ROUTES = {
  HOME: '/',
  SIGN_IN: '/sign-in',
  TASK_LIST: '/task',
  TASK_DETAIL: (id: string) => `/task/${id}`,
  USER: '/user',
} as const;
```

**queryKeys.ts 예시**:
```typescript
// constants/queryKeys.ts
export const QUERY_KEYS = {
  dashboard: ['dashboard'] as const,
  tasks: ['tasks'] as const,
  task: (id: string) => ['task', id] as const,
  user: ['user'] as const,
} as const;
```

---

## 5. Import 규칙 및 Path Alias

### 5.1 Path Alias 설정

```typescript
// tsconfig.json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@components/*": ["src/components/*"],
      "@features/*": ["src/features/*"],
      "@hooks/*": ["src/hooks/*"],
      "@utils/*": ["src/utils/*"],
      "@types/*": ["src/types/*"],
      "@constants/*": ["src/constants/*"],
      "@api/*": ["src/api/*"]
    }
  }
}
```

### 5.2 Import 순서 규칙

```typescript
// 1. React/외부 라이브러리
import { useState, useEffect } from 'react';
import { useQuery } from '@tanstack/react-query';

// 2. 내부 모듈 (절대 경로)
import { Button, Input } from '@/components/common';
import { useTasks } from '@/features/task';
import { ROUTES } from '@/constants/routes';

// 3. 상대 경로 (같은 모듈 내)
import { TaskCardProps } from './TaskCard.types';
import styles from './TaskCard.module.css';
```

### 5.3 ESLint 설정

```javascript
// .eslintrc.js
module.exports = {
  rules: {
    'import/order': [
      'error',
      {
        groups: [
          'builtin',
          'external',
          'internal',
          'parent',
          'sibling',
          'index',
        ],
        pathGroups: [
          { pattern: 'react', group: 'external', position: 'before' },
          { pattern: '@/**', group: 'internal' },
        ],
        'newlines-between': 'always',
        alphabetize: { order: 'asc' },
      },
    ],
  },
};
```

---

## 6. 확장 시나리오

### 6.1 새로운 기능 추가 시

**시나리오**: "알림(Notification)" 기능 추가

```
features/
├── auth/
├── dashboard/
├── task/
├── user/
└── notification/          # 🆕 새 도메인 추가
    ├── components/
    │   ├── NotificationList/
    │   └── NotificationItem/
    ├── hooks/
    │   └── useNotifications.ts
    └── index.ts
```

**영향 범위**:
- 기존 코드 수정 ❌
- 새 폴더만 추가 ✅

---

### 6.2 공통 컴포넌트 추가 시

**시나리오**: "Tooltip" 컴포넌트 추가

```
components/common/
├── Button/
├── Input/
├── Modal/
└── Tooltip/              # 🆕 추가
    ├── Tooltip.tsx
    ├── Tooltip.module.css
    └── index.ts
```

---

### 6.3 API 엔드포인트 추가 시

**시나리오**: Task에 "완료 처리" API 추가

```typescript
// features/task/hooks/useCompleteTask.ts  # 🆕 추가
import { client } from '@/api/client';

export const useCompleteTask = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: string) => client.patch(`/api/task/${id}/complete`),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: QUERY_KEYS.tasks });
    },
  });
};
```

---

### 6.4 규모 확장 시 구조 변화

**Phase 1: 현재 (소규모)**
```
features/task/
├── components/
├── hooks/
├── api/
└── index.ts
```

**Phase 2: 중기 (중규모)**
```
features/task/
├── components/
├── hooks/
├── api/
├── types/              # 🆕 타입 분리
├── utils/              # 🆕 도메인 유틸
├── constants/          # 🆕 도메인 상수
└── index.ts
```

**Phase 3: 장기 (대규모)**
```
features/
├── task/
│   ├── task-list/      # 🆕 서브 도메인 분리
│   │   ├── components/
│   │   ├── hooks/
│   │   └── index.ts
│   ├── task-detail/
│   │   ├── components/
│   │   ├── hooks/
│   │   └── index.ts
│   ├── shared/         # 🆕 task 내 공유
│   │   ├── api/
│   │   └── types/
│   └── index.ts
```

---

## 7. 파일 네이밍 컨벤션

### 7.1 파일명 규칙

| 유형 | 규칙 | 예시 |
|:---|:---|:---|
| 컴포넌트 | PascalCase | `TaskCard.tsx` |
| 훅 | camelCase + use 접두사 | `useTasks.ts` |
| 유틸리티 | camelCase | `validation.ts` |
| 타입 | camelCase 또는 PascalCase | `api.ts`, `Task.types.ts` |
| 상수 | camelCase | `routes.ts`, `queryKeys.ts` |
| 스타일 | 컴포넌트명.module.css | `TaskCard.module.css` |
| 테스트 | 컴포넌트명.test.tsx | `TaskCard.test.tsx` |

### 7.2 폴더명 규칙

| 유형 | 규칙 | 예시 |
|:---|:---|:---|
| 컴포넌트 폴더 | PascalCase | `TaskCard/` |
| 기능 폴더 | kebab-case 또는 camelCase | `task/`, `task-list/` |
| 일반 폴더 | kebab-case | `hooks/`, `utils/` |

---

## 8. 체크리스트

### 8.1 구조 설정

- [ ] Path Alias 설정 (tsconfig.json)
- [ ] ESLint import 규칙 설정
- [ ] 폴더 구조 생성
- [ ] index.ts (barrel exports) 작성

### 8.2 레이어 준수

- [ ] pages → features 방향 의존성
- [ ] features → components 방향 의존성
- [ ] components는 도메인 무관
- [ ] shared는 의존성 없음

### 8.3 모듈 완결성

- [ ] 각 feature는 자체 components, hooks, api 보유
- [ ] Public API (index.ts)를 통한 외부 노출
- [ ] 내부 구조 직접 import 금지

---

## 9. 요약

### 9.1 구조 선택 이유

| 원칙 | 구현 방식 | 이점 |
|:---|:---|:---|
| 관심사 분리 | pages/features/components 레이어 | 책임 명확화 |
| 도메인 응집 | features/{domain}/ | 관련 코드 그룹핑 |
| 재사용성 | components/common/ | UI 일관성 |
| 확장성 | 모듈 단위 추가 | 영향 범위 최소화 |
| 유지보수성 | 코로케이션 | 관련 파일 근접 배치 |

### 9.2 확장 시 가이드

| 상황 | 행동 |
|:---|:---|
| 새 도메인 추가 | `features/{domain}/` 생성 |
| 공통 UI 추가 | `components/common/` 추가 |
| 도메인 API 추가 | `features/{domain}/hooks/`에 API 호출 추가 |
| 범용 훅 추가 | `hooks/` 추가 |
| 도메인 훅 추가 | `features/{domain}/hooks/` 추가 |



### 9.3 주요 포인트

```
Q: "왜 이 구조를 선택했나요?"

A: "역할 기반과 도메인 기반의 장점을 결합한 Feature-Sliced 변형 구조입니다.
   
   1. pages는 라우트와 1:1 매핑되어 진입점 역할만 합니다.
   2. features는 도메인별로 응집도 높은 모듈을 구성합니다.
   3. components는 도메인 무관한 재사용 가능한 UI입니다.
   
   이 구조는 새 기능 추가 시 기존 코드를 수정하지 않고
   features 폴더에 새 도메인만 추가하면 되어 확장성이 좋습니다.
   
   또한 단방향 의존성 규칙을 통해 의존성 순환을 방지하고,
   각 모듈의 Public API를 통해 내부 구현을 캡슐화했습니다."
```

---
